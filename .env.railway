# Railway.com Environment Variables Template
# These should be set in Railway's environment variables section

# Server configuration
NODE_ENV=production
PORT=$PORT

# Session secret (set a secure random string in Railway)
SESSION_SECRET=$SESSION_SECRET

# Email configuration
EMAIL_HOST=$EMAIL_HOST
EMAIL_PORT=$EMAIL_PORT
EMAIL_USER=$EMAIL_USER
EMAIL_PASS=$EMAIL_PASS
EMAIL_FROM=$EMAIL_FROM

# Admin credentials
ADMIN_USERNAME=$ADMIN_USERNAME
ADMIN_PASSWORD=$ADMIN_PASSWORD

# Database configuration (Railway will provide these for PostgreSQL)
DATABASE_URL=$DATABASE_URL
PGHOST=$PGHOST
PGPORT=$PGPORT
PGDATABASE=$PGDATABASE
PGUSER=$PGUSER
PGPASSWORD=$PGPASSWORD

# Railway-specific variables
RAILWAY_ENVIRONMENT=$RAILWAY_ENVIRONMENT
RAILWAY_PROJECT_ID=$RAILWAY_PROJECT_ID
RAILWAY_SERVICE_ID=$RAILWAY_SERVICE_ID